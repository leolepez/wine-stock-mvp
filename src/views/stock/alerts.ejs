<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="csrf-token" content="<%= csrfToken %>">
  <title><%= title %> - Wine Stock MVP</title>
  <link href="/css/output.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
  <%- include('../partials/nav', { active: 'stock' }) %>

  <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
    <div class="mb-6">
      <a href="/stock" class="text-indigo-600 hover:text-indigo-900 text-sm">
        &larr; Volver a inventario
      </a>
    </div>

    <div class="mb-6">
      <h1 class="text-2xl font-semibold text-gray-900">Alertas de Stock Bajo</h1>
      <p class="mt-1 text-sm text-gray-600">Productos con menos de 20 unidades</p>
    </div>

    <% if (alerts.length === 0) { %>
    <div class="bg-green-50 rounded-lg p-8 text-center">
      <svg class="mx-auto h-12 w-12 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <h3 class="mt-2 text-sm font-medium text-green-800">Todo en orden</h3>
      <p class="mt-1 text-sm text-green-600">No hay productos con stock bajo</p>
    </div>
    <% } else { %>
    <div class="bg-white shadow rounded-lg overflow-hidden">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Producto</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Depósito</th>
            <th class="px-4 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Stock Actual</th>
            <th class="px-4 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Acción</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% alerts.forEach(alert => { %>
          <tr class="<%= alert.quantity === 0 ? 'bg-red-50' : 'bg-yellow-50' %>">
            <td class="px-4 py-3">
              <div class="text-sm font-medium text-gray-900"><%= alert.product.name %></div>
              <div class="text-xs text-gray-500"><%= alert.product.sku %></div>
            </td>
            <td class="px-4 py-3 text-sm text-gray-900">
              <%= alert.warehouse.name %>
            </td>
            <td class="px-4 py-3 text-center">
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-sm font-medium <%= alert.quantity === 0 ? 'bg-red-100 text-red-800' : 'bg-yellow-100 text-yellow-800' %>">
                <%= alert.quantity %>
              </span>
            </td>
            <td class="px-4 py-3 text-right">
              <a href="/stock/adjust?productId=<%= alert.product.id %>&warehouseId=<%= alert.warehouse.id %>"
                 class="text-indigo-600 hover:text-indigo-900 text-sm">
                Ajustar
              </a>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } %>
  </main>
</body>
</html>
