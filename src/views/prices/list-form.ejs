<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="csrf-token" content="<%= csrfToken %>">
  <title><%= title %> - Wine Stock MVP</title>
  <link href="/css/output.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
  <% if (user) { %>
  <nav class="bg-white shadow-sm border-b">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex space-x-8">
          <a href="/products" class="inline-flex items-center px-1 pt-1 text-gray-600 hover:text-gray-900">Productos</a>
          <a href="/prices" class="inline-flex items-center px-1 pt-1 border-b-2 border-indigo-500 text-gray-900 font-medium">Precios</a>
        </div>
        <div class="flex items-center">
          <span class="text-sm text-gray-700 mr-4"><%= user.email %> (<%= user.role %>)</span>
          <form action="/auth/logout" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <button class="text-sm text-red-600 hover:text-red-800">Salir</button>
          </form>
        </div>
      </div>
    </div>
  </nav>
  <% } %>

  <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
    <div class="mb-6">
      <a href="/prices" class="text-indigo-600 hover:text-indigo-900 text-sm">
        &larr; Volver a listas de precios
      </a>
    </div>

    <div class="bg-white shadow rounded-lg max-w-lg">
      <div class="px-4 py-5 sm:p-6">
        <h2 class="text-lg font-medium text-gray-900 mb-6"><%= title %></h2>

        <% if (errors && errors.length > 0) { %>
        <div class="rounded-md bg-red-50 p-4 mb-6">
          <ul class="list-disc list-inside text-sm text-red-700">
            <% errors.forEach(error => { %>
            <li><%= error.msg %></li>
            <% }) %>
          </ul>
        </div>
        <% } %>

        <form action="<%= priceList && priceList.id ? '/prices/' + priceList.id : '/prices' %>" method="POST" class="space-y-6">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">

          <div>
            <label for="name" class="block text-sm font-medium text-gray-700">Nombre *</label>
            <input type="text"
                   name="name"
                   id="name"
                   value="<%= priceList ? priceList.name : '' %>"
                   required
                   placeholder="ej: General, Mayorista, Distribuidor"
                   class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm px-3 py-2 border">
          </div>

          <div>
            <label class="flex items-center">
              <input type="checkbox"
                     name="active"
                     value="true"
                     <%= (!priceList || priceList.active) ? 'checked' : '' %>
                     class="rounded border-gray-300 text-indigo-600 shadow-sm focus:border-indigo-500 focus:ring-indigo-500">
              <span class="ml-2 text-sm text-gray-700">Lista activa</span>
            </label>
            <p class="mt-1 text-xs text-gray-500">Las listas inactivas no se muestran en cotizaciones</p>
          </div>

          <div class="flex justify-end space-x-3">
            <a href="/prices"
               class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50">
              Cancelar
            </a>
            <button type="submit"
                    class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700">
              <%= priceList && priceList.id ? 'Guardar Cambios' : 'Crear Lista' %>
            </button>
          </div>
        </form>
      </div>
    </div>
  </main>
</body>
</html>
